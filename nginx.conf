# Nginx configuration for Doeng World
# Place this in: /etc/nginx/sites-available/doeng.world

server {
    listen 80;
    listen [::]:80;
    
    server_name doeng.world www.doeng.world;
    
    root /var/www/doeng.world;
    index index.html;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # Main location
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache static assets
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Deny access to hidden files
    location ~ /\. {
        deny all;
    }
}

# SSL configuration (uncomment after setting up SSL with certbot)
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     
#     server_name doeng.world www.doeng.world;
#     
#     ssl_certificate /etc/letsencrypt/live/doeng.world/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/doeng.world/privkey.pem;
#     
#     root /var/www/doeng.world;
#     index index.html;
#     
#     # ... (copy the same configuration from above)
# }
